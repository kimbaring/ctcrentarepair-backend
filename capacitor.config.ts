import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'RentARepairUI',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
